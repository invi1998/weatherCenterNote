# 数据入库子程序

## 解析入库参数

```c++
// 业务处理主函数
bool _xmltodb()
{
    // 把数据入库的参数配置文件starg.inifilename加载到容器中

    while (true)
    {
        // 打开starg.xmlpath目录

        
        // 遍历目录中的文件名
        while(true)
        {
            // 读取目录，得到一个xml文件

            // 处理xml文件
            // 调用文件处理子函数

            // 处理xml文件成功，写日志，备份文件

            // 处理文件失败。分情况讨论
        }

        sleep(starg.timetvl);
    }
    

    return true;
}
```

